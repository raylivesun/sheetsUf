// the following disclaimer in the documentation and/or other materials
// provided with the distribution.
var topics = {
  "http://www.w3.org/2001/XMLSchema": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-instance": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-datatypes": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-instance-datatypes": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-core": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-instance-core": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-datatypes-core": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-instance-datatypes-core": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-core-schema-instance": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-instance-core-schema-instance": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-datatypes-core-schema-instance": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-instance-datatypes-core-schema-instance": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-core-schema-instance-core": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-instance-core-schema-instance-core": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-datatypes-core-schema-instance-core": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-instance-datatypes-core-schema-instance-core": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-core-schema-instance-datatypes": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-instance-core-schema-instance-datatypes": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-datatypes-core-schema-instance-datatypes": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-instance-datatypes-core-schema-instance-datatypes": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-core-schema-instance-core-schema-instance": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-instance-core-schema-instance-core-schema-instance": ['documentation'],
  "http://www.w3.org/2001/XMLSchema-datatypes-core-schema-instance-core-schema-instance": ['documentation'],  
};
// Create the schema instance
Map<String, List<String>> getTopic(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// Create the schema instance
Map<String, List<String>> getTopicList(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// Create the schema instance
Map<String, List<String>> getTopicMap(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
// topics list event
Map<String, List<String>> getTopicMapList(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapEvent(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}


// the program list stream
Map<String, List<String>> getTopicMapEventList(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapsEvent(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapsList(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapLink(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreams(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}


// the program list stream
Map<String, List<String>> getTopicMapsStreams(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamsEvent(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamsList(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamsLink(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamsTypes(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamsLocal(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamAuto(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamLogic(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamList(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamLink(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamRules(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStream(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamStates(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamStatic(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamLocal(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}


// the program list stream
Map<String, List<String>> getTopicMapStreamForm(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamUsing(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamUsages(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamMoved(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamMove(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamPush(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamPull(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamPoll(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamPublish(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}


// the program list stream
Map<String, List<String>> getTopicMapStreamUtils(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamBuffer(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}
  
// the program list stream
Map<String, List<String>> getTopicMapStreamSaved(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicStreamSave(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamGeneral(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamPublic(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamPool(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamInteger(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamBoolean(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamChilds(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamSons(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamPattern(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamFamily(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamCountry(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamCity(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamMunicipals(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamBarriers(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStreamStudy(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapCreate(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapAuto(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapDestructor(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapConstructor(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapEventAutoDevelop(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapDevelop(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapSites(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapWebsite(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}


// the program list stream
Map<String, List<String>> getTopicMapPopup(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapIPC(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapIPS(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicIP(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapCycles(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapLoop(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapUntils(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapDelivery(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapString(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapDispositive(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapExplores(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapGenerators(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapFiles(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapFilters(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapMachines(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapNotebooks(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapPlack(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapCating(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapMessages(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapTalking(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapDell(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}


// the program list stream
Map<String, List<String>> getTopicMapDellTech(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapCast(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapVariables(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStructures(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapClass(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapPool(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapCofees(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapDinners(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapLunch(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapLaunch(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapAddress(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapBooks(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapPDF(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapLibrary(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapStudies(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapFactory(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapFactor(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapArms(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}


// the program list stream
Map<String, List<String>> getTopicMapSituations(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapNeeds(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapClouds(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapFunction(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapSafes(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapCools(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapMath(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapAlgorithm(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapKeys(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapExp(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapCos(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapSin(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapCosh(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapSinh(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapACos(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapASin(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapACosh(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapEventASinh(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapElements(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapElement(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapPhysics(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapSciences(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapGeographic(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapDynamics(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapTokens(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}

// the program list stream
Map<String, List<String>> getTopicMapLocal(uri) {
  if (uri.startsWith (" questions ")) {
      print(topics);
  }
  return topics;
}









