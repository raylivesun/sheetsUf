// the following disclaimer in the documentation and/or other materials
// provided with the distribution.


Future<List> ILoop (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopList (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMap (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapList (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapEvent (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapEventList (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapEventLink (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


// ----------------------------------------------------------------
Future<List> ILoopMapEventRun (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapStreams (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapCurrent (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


// ----------------------------------------------------------------
Future<List> ILoopMapSates (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapUsages (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapFiles (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapConnect (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


// ----------------------------------------------------------------
Future<List> ILoopMapConnected (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapConnections (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapConnection (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupsConnect (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupConnect (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


// ----------------------------------------------------------------
Future<List> ILoopMapGroupsConnected (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}



// ----------------------------------------------------------------
Future<List> ILoopMapEventGroupConnected (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupsNetworks (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupNetwork (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


// ----------------------------------------------------------------
Future<List> ILoopMapGroupLink (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupPublish (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupSettings (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupSelection (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupSelect (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


// ----------------------------------------------------------------
Future<List> ILoopMapGroupTypes (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupPhysics (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupFriends (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupAnalysis (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


// ----------------------------------------------------------------
Future<List> ILoopMapGroupStatic (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupFunction (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupVeraibles (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupElements (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupArray (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}



// ----------------------------------------------------------------
Future<List> ILoopMapGroupSet (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupGet (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupBenefit (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


// ----------------------------------------------------------------
Future<List> ILoopMapGroupCreate (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupConstructor (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupDestructor (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupStates (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupCountry (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


// ----------------------------------------------------------------
Future<List> ILoopMapGroupCommunity (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupCity (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupMunicipals (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

// ----------------------------------------------------------------
Future<List> ILoopMapGroupBarrier (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


// ----------------------------------------------------------------
Future<List> ILoopMapGroupContainers (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupMath (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupScience (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupGeographic (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


//----------------------------------------------------------------
Future<List> ILoopMapGroupDynamic (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupUntils (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupPolicy (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupOptional (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupLocal (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


//----------------------------------------------------------------
Future<List> ILoopMapGroupFamily (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupPattern (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupDefault (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupStudy (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


//----------------------------------------------------------------
Future<List> ILoopMapGroupStudies (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupTopics (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupReaders (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupWrites (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


//----------------------------------------------------------------
Future<List> ILoopMapGroupPolitics (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupForms (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupDialog (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupStart (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupPlay (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


//----------------------------------------------------------------
Future<List> ILoopMapGroupThemes (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapGroupFireworks (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}



//----------------------------------------------------------------
Future<List> ILoopMapFireworksLocal (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapFireworkStatic (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapFireworkLink (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapFireworkEvent (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapFireworkImages (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapFireworkPush (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapFireworkPull (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}

//----------------------------------------------------------------
Future<List> ILoopMapFireworkConnect (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}


//----------------------------------------------------------------
Future<List> ILoopMapFireworkNetworks (List<Function> functions) async {
  for (var OpenAI in functions) {
    await OpenAI();
  }
    return await Future.wait(functions as Iterable<Future>);
}



